import { DynamicFormModels } from './dynamic-form-models';

describe('DynamicFormModels', () => {
  it('should create an instance', () => {
    expect(new DynamicFormModels()).toBeTruthy();
  });
});
